module.exports = function sum(number1, number2) {
    // After step3 failed, it's time to refactor.
    return number1 + number2;
    // Now we are telling that sum function has to sum two numbers that will be
    // defined.
}
